import { HumanizedFormErrorDirective } from './humanized-form-error.directive';

describe('HumanizedFormErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new HumanizedFormErrorDirective();
    expect(directive).toBeTruthy();
  });
});
